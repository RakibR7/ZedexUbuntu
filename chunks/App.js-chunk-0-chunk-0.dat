
import React, { useState } from 'react';
import './App.css'; // Make sure you have your CSS styles in App.css

function App() {
  const [file, setFile] = useState(null);
  const [fileName, setFileName] = useState('No file chosen');

  const handleFileChange = (event) => {
    setFile(event.target.files[0]);
    setFileName(event.target.files[0] ? event.target.files[0].name : 'No file chosen');
  };

  const handleUpload = () => {
    // Implement the upload logic here
    // This could involve sending the file to a backend server
    console.log('File to upload:', file);
    alert('File upload functionality not implemented yet');
  };

  return (
    <div className="App">
      <header className="App-header">
        <h1>Discord-Themed File Upload</h1>
        <div className="file-upload-container">
          <input type="file" id="fileInput" hidden onChange={handleFileChange} />
          <button onClick={() => document.getElementById('fileInput').click()}>Choose File</button>
          <span>{fileName}</span>
          <button onClick={handleUpload}>Upload</button>
        </div>
      </header>
    </div>
  );
}

export default App;

/* App.css */
.App {
  text-align: center;
}

.App-header {
  background-color: #23272A;
  color: white;
  padding: 20px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.file-upload-container {
  margin-top: 20px;
}

button {
  background-color: #7289DA;
  border: none;
  border-radius: 4px;
  color: white;
  padding: 10px 20px;
  margin: 5px;
  cursor: pointer;
  transition: background-color 0.3s;
}

button:hover {
  background-color: #677BC4;
}

span {
  color: #99AAB5;
}
